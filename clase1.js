//en mayusculas
var nombre = 'daniel'
var apellido = 'CASTRO'

var newName = nombre.toUpperCase();
var newSurname = apellido.toLowerCase();
var primeraLetradelNombre = nombre.charAt(1);
var cantidadDeCaracteres = nombre.length;

var nombreCompleto = `${nombre} ${apellido.toLowerCase()}`//interpolar variables

var str = nombre.charAt(1) + nombre.charAt(2)

var substr = nombre.substr(1,3)//desde la posicion 1, 3 caracteres
